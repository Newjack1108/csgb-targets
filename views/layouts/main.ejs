<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'CSGB Targets' %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <div class="container">
            <h1>CSGB Targets</h1>
            <% if (typeof user !== 'undefined' && user) { %>
                <nav>
                    <span class="user-info"><%= user.name %> (<%= user.role %>)</span>
                    <% if (user.role === 'sales' || user.role === 'director') { %>
                        <a href="/sales/dashboard">Sales Dashboard</a>
                    <% } %>
                    <% if (user.role === 'production' || user.role === 'director') { %>
                        <a href="/production/dashboard">Production Dashboard</a>
                    <% } %>
                    <% if (user.role === 'director') { %>
                        <a href="/settings">Settings</a>
                        <a href="/users">Users</a>
                    <% } %>
                    <form method="POST" action="/logout" style="display: inline;">
                        <button type="submit" class="btn-link">Logout</button>
                    </form>
                </nav>
            <% } %>
        </div>
    </header>

    <main class="container">
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-error"><%= error %></div>
        <% } %>
        <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success">Operation successful</div>
        <% } %>

        <%- body %>
    </main>

    <footer>
        <div class="container">
            <p>We target boxes and margin. Everything else explains the result.</p>
        </div>
    </footer>
</body>
</html>
